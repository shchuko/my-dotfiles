# Dotfiles

Personal dotfiles for [CodeCanvas](https://www.jetbrains.com/codecanvas/) dev environments.

CodeCanvas clones this repo into `~/dotfiles` and runs `bootstrap.sh` on environment start.

All scripts are idempotent — safe to run multiple times.
